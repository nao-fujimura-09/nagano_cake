<div class="container">
  <h4 class="m-5 d-inline-block px-3" style="background-color:#d3d3d3">商品詳細</h4>
  
  <div class="row justify-content-center">
    <div class="pr-5" align="left">
      <%= image_tag @item.get_item_image(100,60) %>
    </div>
  
    <table width="30%">
      <tbody>
        <tr>
            <td>商品名</td>
            <td><%= @item.name %></td>
        </tr>
        <tr>
          <td>商品説明</td>
          <td><%= @item.introduction %></td>
        </tr>
        <tr>
          <td>ジャンル</td>
          <td><%= @item.genre.name %></td>
        </tr>
        <tr>
          <td>税込価格<br>(税抜価格)</td>
          <td>
            <%= @item.add_tax_price.to_s(:delimited) %>
            (<%= @item.price.to_s(:delimited) %>) 円
          </td>
        </tr>
        <tr>
          <td>販売ステータス</td>
          <td>
            <% if @item.is_active? %> 
              <p class="font-weight-bold" style="color:#32cd32">販売中</p>
            <% else %>
              <p class="font-weight-bold" style="color:#d3dd3d3">販売停止中</p>
            <% end %>
          </td>
        </tr>
      </tbody>
    </table>
  </div>
  <div align="center">
    <%= link_to "編集する", edit_admin_item_path, class:"btn btn-success my-5 py-2 px-5" %>
  </div>
</div>